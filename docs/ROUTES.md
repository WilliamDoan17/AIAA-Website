# ROUTES (suggestion)

This document will organize application routes for AIAA-Website. Group routes by feature or area, detailing their naming and access rules.

Example structure:

- /projects (public)
- /projects/:id (public)
- /admin/members (admin-only)
- /admin/projects (admin-only)
- /events (public)
- /contributor/projects (contributors)

(Add actual and additional route groupings as needed.)